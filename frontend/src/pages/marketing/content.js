/**
 * Legacy content registry for ContentPage.jsx.
 *
 * The public website now uses dedicated services-led pages instead of the old
 * SaaS/product registry. Keep this export so ContentPage can fail safely if an
 * old import path is restored later.
 */
export const PAGES = {};
